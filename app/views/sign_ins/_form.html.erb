<%= form_with(model: sign_in) do |form| %>
  <% if sign_in.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sign_in.errors.count, "error") %> prohibited this sign_in from being saved:</h2>

      <ul>
        <% sign_in.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
