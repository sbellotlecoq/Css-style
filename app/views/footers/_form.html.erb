<%= form_with(model: footer) do |form| %>
  <% if footer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(footer.errors.count, "error") %> prohibited this footer from being saved:</h2>

      <ul>
        <% footer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
